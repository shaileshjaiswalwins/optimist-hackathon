// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 2.10.0 (commit baca5cdf77577ed4e3f30da48a5158189c4ea43f).

/* eslint-disable */
/* tslint:disable */
import {
  DbConnectionBuilder as __DbConnectionBuilder,
  DbConnectionImpl as __DbConnectionImpl,
  SubscriptionBuilderImpl as __SubscriptionBuilderImpl,
  TypeBuilder as __TypeBuilder,
  Uuid as __Uuid,
  convertToAccessorMap as __convertToAccessorMap,
  makeQueryBuilder as __makeQueryBuilder,
  procedureSchema as __procedureSchema,
  procedures as __procedures,
  reducerSchema as __reducerSchema,
  reducers as __reducers,
  schema as __schema,
  t as __t,
  table as __table,
  type AlgebraicTypeType as __AlgebraicTypeType,
  type DbConnectionConfig as __DbConnectionConfig,
  type ErrorContextInterface as __ErrorContextInterface,
  type Event as __Event,
  type EventContextInterface as __EventContextInterface,
  type Infer as __Infer,
  type QueryBuilder as __QueryBuilder,
  type ReducerEventContextInterface as __ReducerEventContextInterface,
  type RemoteModule as __RemoteModule,
  type SubscriptionEventContextInterface as __SubscriptionEventContextInterface,
  type SubscriptionHandleImpl as __SubscriptionHandleImpl,
} from "spacetimedb";

// Import all reducer arg schemas
import EnsureWaitingMatchReducer from "./ensure_waiting_match_reducer";
import JoinMatchReducer from "./join_match_reducer";
import SetDrivingInputReducer from "./set_driving_input_reducer";
import StartMatchReducer from "./start_match_reducer";

// Import all procedure arg schemas

// Import all table schema definitions
import CollisionEventRow from "./collision_event_table";
import MatchRow from "./match_table";
import ObstacleRow from "./obstacle_table";
import PlayerPositionRow from "./player_position_table";
import PlayerProfileRow from "./player_profile_table";
import PlayerVitalsRow from "./player_vitals_table";

/** Type-only namespace exports for generated type groups. */

/** The schema information for all tables in this module. This is defined the same was as the tables would have been defined in the server. */
const tablesSchema = __schema({
  collisionEvent: __table({
    name: 'collision_event',
    indexes: [
    ],
    constraints: [
    ],
    event: true,
  }, CollisionEventRow),
  match: __table({
    name: 'match',
    indexes: [
      { accessor: 'match_id', name: 'match_match_id_idx_btree', algorithm: 'btree', columns: [
        'matchId',
      ] },
    ],
    constraints: [
      { name: 'match_match_id_key', constraint: 'unique', columns: ['matchId'] },
    ],
  }, MatchRow),
  obstacle: __table({
    name: 'obstacle',
    indexes: [
      { accessor: 'match_id', name: 'obstacle_match_id_idx_btree', algorithm: 'btree', columns: [
        'matchId',
      ] },
      { accessor: 'obstacle_id', name: 'obstacle_obstacle_id_idx_btree', algorithm: 'btree', columns: [
        'obstacleId',
      ] },
    ],
    constraints: [
      { name: 'obstacle_obstacle_id_key', constraint: 'unique', columns: ['obstacleId'] },
    ],
  }, ObstacleRow),
  playerPosition: __table({
    name: 'player_position',
    indexes: [
      { accessor: 'match_id', name: 'player_position_match_id_idx_btree', algorithm: 'btree', columns: [
        'matchId',
      ] },
      { accessor: 'player_id', name: 'player_position_player_id_idx_btree', algorithm: 'btree', columns: [
        'playerId',
      ] },
    ],
    constraints: [
      { name: 'player_position_player_id_key', constraint: 'unique', columns: ['playerId'] },
    ],
  }, PlayerPositionRow),
  playerProfile: __table({
    name: 'player_profile',
    indexes: [
      { accessor: 'match_id', name: 'player_profile_match_id_idx_btree', algorithm: 'btree', columns: [
        'matchId',
      ] },
      { accessor: 'player_id', name: 'player_profile_player_id_idx_btree', algorithm: 'btree', columns: [
        'playerId',
      ] },
    ],
    constraints: [
      { name: 'player_profile_player_id_key', constraint: 'unique', columns: ['playerId'] },
    ],
  }, PlayerProfileRow),
  playerVitals: __table({
    name: 'player_vitals',
    indexes: [
      { accessor: 'match_id', name: 'player_vitals_match_id_idx_btree', algorithm: 'btree', columns: [
        'matchId',
      ] },
      { accessor: 'player_id', name: 'player_vitals_player_id_idx_btree', algorithm: 'btree', columns: [
        'playerId',
      ] },
    ],
    constraints: [
      { name: 'player_vitals_player_id_key', constraint: 'unique', columns: ['playerId'] },
    ],
  }, PlayerVitalsRow),
});

/** The schema information for all reducers in this module. This is defined the same way as the reducers would have been defined in the server, except the body of the reducer is omitted in code generation. */
const reducersSchema = __reducers(
  __reducerSchema("ensure_waiting_match", EnsureWaitingMatchReducer),
  __reducerSchema("join_match", JoinMatchReducer),
  __reducerSchema("set_driving_input", SetDrivingInputReducer),
  __reducerSchema("start_match", StartMatchReducer),
);

/** The schema information for all procedures in this module. This is defined the same way as the procedures would have been defined in the server. */
const proceduresSchema = __procedures(
);

type __SchemaWithTableAccessorAliases = Omit<typeof tablesSchema.schemaType, "tables"> & {
  tables: typeof tablesSchema.schemaType.tables & {
    /** @deprecated Use `collisionEvent` instead. This alias will be removed in the next major version. */
    readonly "collision_event": Omit<typeof tablesSchema.schemaType.tables["collisionEvent"], "accessorName"> & { readonly accessorName: "collision_event" };
    /** @deprecated Use `playerPosition` instead. This alias will be removed in the next major version. */
    readonly "player_position": Omit<typeof tablesSchema.schemaType.tables["playerPosition"], "accessorName"> & { readonly accessorName: "player_position" };
    /** @deprecated Use `playerProfile` instead. This alias will be removed in the next major version. */
    readonly "player_profile": Omit<typeof tablesSchema.schemaType.tables["playerProfile"], "accessorName"> & { readonly accessorName: "player_profile" };
    /** @deprecated Use `playerVitals` instead. This alias will be removed in the next major version. */
    readonly "player_vitals": Omit<typeof tablesSchema.schemaType.tables["playerVitals"], "accessorName"> & { readonly accessorName: "player_vitals" };
  };
};

/** The remote SpacetimeDB module schema, both runtime and type information. */
const REMOTE_MODULE = {
  versionInfo: {
    cliVersion: "2.10.0" as const,
  },
  tables: tablesSchema.schemaType.tables as __SchemaWithTableAccessorAliases["tables"],
  reducers: reducersSchema.reducersType.reducers,
  ...proceduresSchema,
} satisfies __RemoteModule<
  __SchemaWithTableAccessorAliases,
  typeof reducersSchema.reducersType,
  typeof proceduresSchema
>;

const tableAccessorAliases = {
  "collision_event": "collisionEvent",
  "player_position": "playerPosition",
  "player_profile": "playerProfile",
  "player_vitals": "playerVitals",
} as const;

function __withTableAccessorAliases<T extends object>(target: T, freeze = false): T {
  const out = Object.create(Object.getPrototypeOf(target)) as T & Record<string, unknown>;
  Object.defineProperties(out, Object.getOwnPropertyDescriptors(target));
  for (const [deprecatedAccessor, targetAccessor] of Object.entries(tableAccessorAliases)) {
    if (deprecatedAccessor in out) {
      continue;
    }
    Object.defineProperty(out, deprecatedAccessor, {
      enumerable: true,
      configurable: false,
      get: () => out[targetAccessor],
    });
  }
  return freeze ? Object.freeze(out) : out;
}

type __DbViewBase = __DbConnectionImpl<typeof REMOTE_MODULE>["db"];
export type DbView = __DbViewBase & {
  /** @deprecated Use `collisionEvent` instead. This alias will be removed in the next major version. */
  readonly "collision_event": __DbViewBase["collisionEvent"];
  /** @deprecated Use `playerPosition` instead. This alias will be removed in the next major version. */
  readonly "player_position": __DbViewBase["playerPosition"];
  /** @deprecated Use `playerProfile` instead. This alias will be removed in the next major version. */
  readonly "player_profile": __DbViewBase["playerProfile"];
  /** @deprecated Use `playerVitals` instead. This alias will be removed in the next major version. */
  readonly "player_vitals": __DbViewBase["playerVitals"];
};

type __TablesBase = __QueryBuilder<typeof tablesSchema.schemaType>;
export type Tables = __TablesBase & {
  /** @deprecated Use `collisionEvent` instead. This alias will be removed in the next major version. */
  readonly "collision_event": __TablesBase["collisionEvent"];
  /** @deprecated Use `playerPosition` instead. This alias will be removed in the next major version. */
  readonly "player_position": __TablesBase["playerPosition"];
  /** @deprecated Use `playerProfile` instead. This alias will be removed in the next major version. */
  readonly "player_profile": __TablesBase["playerProfile"];
  /** @deprecated Use `playerVitals` instead. This alias will be removed in the next major version. */
  readonly "player_vitals": __TablesBase["playerVitals"];
};

/** The tables available in this remote SpacetimeDB module. Each table reference doubles as a query builder. */
const tablesBase: __TablesBase = __makeQueryBuilder(tablesSchema.schemaType);
export const tables: Tables = __withTableAccessorAliases(tablesBase, true) as Tables;

/** The reducers available in this remote SpacetimeDB module. */
export const reducers = __convertToAccessorMap(reducersSchema.reducersType.reducers);

/** The procedures available in this remote SpacetimeDB module. */
export const procedures = __convertToAccessorMap(proceduresSchema.procedures);

/** The context type returned in callbacks for all possible events. */
export type EventContext = Omit<__EventContextInterface<typeof REMOTE_MODULE>, "db"> & { db: DbView };
/** The context type returned in callbacks for reducer events. */
export type ReducerEventContext = Omit<__ReducerEventContextInterface<typeof REMOTE_MODULE>, "db"> & { db: DbView };
/** The context type returned in callbacks for subscription events. */
export type SubscriptionEventContext = Omit<__SubscriptionEventContextInterface<typeof REMOTE_MODULE>, "db"> & { db: DbView };
/** The context type returned in callbacks for error events. */
export type ErrorContext = Omit<__ErrorContextInterface<typeof REMOTE_MODULE>, "db"> & { db: DbView };
/** The subscription handle type to manage active subscriptions created from a {@link SubscriptionBuilder}. */
export type SubscriptionHandle = __SubscriptionHandleImpl<typeof REMOTE_MODULE>;

/** Builder class to configure a new subscription to the remote SpacetimeDB instance. */
export class SubscriptionBuilder extends __SubscriptionBuilderImpl<typeof REMOTE_MODULE> {}

/** Builder class to configure a new database connection to the remote SpacetimeDB instance. */
export class DbConnectionBuilder extends __DbConnectionBuilder<DbConnection> {}

/** The typed database connection to manage connections to the remote SpacetimeDB instance. This class has type information specific to the generated module. */
export class DbConnection extends __DbConnectionImpl<typeof REMOTE_MODULE> {
  declare db: DbView;

  constructor(config: __DbConnectionConfig<typeof REMOTE_MODULE>) {
    super(config);
    this.db = __withTableAccessorAliases(this.db) as DbView;
  }

  /** Creates a new {@link DbConnectionBuilder} to configure and connect to the remote SpacetimeDB instance. */
  static builder = (): DbConnectionBuilder => {
    return new DbConnectionBuilder(REMOTE_MODULE, (config: __DbConnectionConfig<typeof REMOTE_MODULE>) => new DbConnection(config));
  };

  /** Creates a new {@link SubscriptionBuilder} to configure a subscription to the remote SpacetimeDB instance. */
  override subscriptionBuilder = (): SubscriptionBuilder => {
    return new SubscriptionBuilder(this);
  };
}

